# Plusly Build #475 - Error Analysis

## Build Status: ❌ FAILED

## Error Summary

**Build:** #475 (Attempt 1)
**Date:** 2026-04-26 12:19 UTC
**Duration:** ~7 minutes 21 seconds

## Critical Error

```
lib/utils/init_with_restore.dart:78:34: Error: Member not found: 'initialized'.
```

## Root Cause

The code references `InitState.initialized` which no longer exists in the Matrix SDK version being used. The `InitState` enum from the `matrix` package has been updated and the `initialized` member has been removed or renamed.

## Affected Code

File: `lib/utils/init_with_restore.dart` (line 78)
```dart
onInitStateChanged: (state) {
  if (state == InitState.migratingDatabase) {
    Logs().i('Database migration started - this may take a while');
    onMigration?.call();
  }
  if (state == InitState.initialized) {  // <-- ERROR: Member not found
    Logs().i('Client initialization completed successfully');
  }
},
```

## Impact

- Build cannot complete
- No APK is generated
- Release cannot be created

## Fix Required

Remove or replace the `InitState.initialized` check. The initialization completion can be detected by the successful completion of the `init()` method instead of relying on the state callback.

## Additional Notes

- This is likely due to a Matrix SDK update where the `InitState` enum was refactored
- The `migratingDatabase` state still exists and works correctly
- Need to check the Matrix SDK changelog for `InitState` changes

---
*Analysis generated by Robbie 🤖*
